<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_main"
    app:startDestination="@id/tripsFragment">

    <fragment
        android:id="@+id/tripsFragment"
        android:name="com.rjdeleon.tourista.old.trips.TripsFragment"
        tools:layout="@layout/fragment_trips"
        android:label="@string/trips_title">
        <action
            android:id="@+id/action_tripsFragment_to_tripIndivFragment"
            app:destination="@id/tripIndivFragment" />
    </fragment>
    <fragment
        android:id="@+id/tripIndivFragment"
        android:name="com.rjdeleon.tourista.old.tripIndiv.TripIndivFragment"
        tools:layout="@layout/fragment_trip_indiv" >
        <argument
            android:name="tripId"
            android:defaultValue="0L" />
        <action
            android:id="@+id/action_tripIndivFragment_to_destinationFragment"
            app:destination="@id/destinationFragment" />
    </fragment>
    <fragment
        android:id="@+id/destinationFragment"
        android:name="com.rjdeleon.tourista.old.destination.DestinationFragment"
        tools:layout="@layout/fragment_destination" >
        <argument
            android:name="id"
            android:defaultValue="0L" />
        <argument
            android:name="tripId"
            android:defaultValue="0L" />
        <action
            android:id="@+id/action_destinationFragment_to_timeZonePickerFragment"
            app:destination="@id/timeZonePickerFragment" />
    </fragment>
    <fragment
        android:id="@+id/timeZonePickerFragment"
        android:name="com.rjdeleon.tourista.old.timeZonePicker.TimeZonePickerFragment"
        tools:layout="@layout/fragment_time_zone_picker" />
</navigation>